# build artifacts (regenerated by `python setup.py build_ext --inplace`)
build/
*.so

# virtualenv
.venv/
# isolated vLLM env (torch 2.11/cu13) — kept separate from the MSAQ build env
.venv-vllm/

# python / pytest caches
__pycache__/
*.pyc
.pytest_cache/

# Claude Code local session settings
.claude/
