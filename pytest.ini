[pytest]
# Put the repo root on sys.path so `import ms_lib` resolves no matter how pytest
# is invoked (`pytest`, `python -m pytest`, from any cwd). tests/conftest.py is
# auto-added to the path by pytest, so `from conftest import ...` also works.
pythonpath = .
testpaths = tests
addopts = -q